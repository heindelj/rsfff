"""rsfff active learning: uncertainty-driven sampling, labeling through a cc_workers store,
committee training. See ../README.md and the plan on the RSFFF board."""

import os

os.environ.setdefault("KMP_DUPLICATE_LIB_OK", "TRUE")

__version__ = "0.1.0"
